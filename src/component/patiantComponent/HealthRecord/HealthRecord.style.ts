export const paper =() =>({
    padding: 2,
    borderRadius: 2,
    boxShadow: 2, 
    border: `1px solid #E0E0E0`, 
    width: '100%', 
    height: '100%',
    display: 'flex', 
    flexDirection: 'column',
    marginBottom:'10px' 
});
export const box1 =() =>({
    borderBottom: '2px solid #050C9C', 
    pb: 1, 
    width: '100%'
});
export const box2 =() =>({
    display: 'flex', 
    flexDirection: 'column', 
    gap: 1, 
    width: '100%' 
});
export const typography =() =>({
    color: '#5A5A5A', 
    display: 'flex', 
    alignItems: 'center'  
});